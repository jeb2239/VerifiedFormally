#include <stdio.h>

int(pre(x > 0)
        post(x>0)
            conditions)(int x)
{
    x = x + 1;
    return x;
}

int 
(post(x==0)
main)()
{
    
    
    
    conditions(5);
   
    return x;
    // For checking whether we can extract conditions from code
}
